import { Component } from '@angular/core';

@Component({
    selector: 'app-settings',
    standalone: true,
    imports: [],
    templateUrl: './settings.route.html',
    styleUrl: './settings.route.less',
})
export class SettingsRoute {}
